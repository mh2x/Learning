@inject NavigationManager Navigation
@code {
    protected override void OnInitialized()
    {
        //BUGBUG: open redirect vulnerability
        //avoid returnurl
        Navigation.NavigateTo($"authentication/login?returnUrl={Navigation.Uri}");
    }
}